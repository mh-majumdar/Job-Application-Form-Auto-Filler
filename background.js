// Background script for the extension
// This can be used for future features like automatic detection

chrome.runtime.onInstalled.addListener(() => {
  console.log('Job Form Auto-Filler extension installed!');
});